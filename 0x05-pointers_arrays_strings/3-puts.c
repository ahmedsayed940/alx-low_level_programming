#include "main.h"
/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: a string
 * Return: equal to length of a string
 */
void _puts(char *str);
{
	int n = 0;

	while (*s != '\0')
	{
		_putchar('*str');
		s++;
		n++;
	}
	_putchar('\n');
}
